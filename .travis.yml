language: r
sudo: false
cache: packages

addons:
  apt:
    packages:
      - libproj-dev
      - libgeos-dev
      - libgdal-dev
      - netcdf-bin
      - libnetcdf-dev
      - python3-pip
      - python3-venv
      - python3-virtualenv

before_install:
  - pip3 install xarray

after_success:
    - Rscript -e 'covr::codecov()'
